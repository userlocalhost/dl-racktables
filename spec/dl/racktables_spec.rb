require 'spec_helper'

describe Dl::Racktables do
  it 'has a version number' do
    expect(Dl::Racktables::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
